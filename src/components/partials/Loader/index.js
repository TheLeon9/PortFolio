import React, { useState, useEffect } from 'react';
import style from './index.module.scss';

const SectionPresentation = () => {
  useEffect(() => {

  }, []);

  return (
    <div className={style.loading_cont}>
      <p>Loader</p>
    </div>
  );
};

export default SectionPresentation;
