import React from 'react';
import style from './index.module.scss';

const SectionAbout= () => {
  return (
    <section className={style.section_about_cont}>

    </section>
  );
};

export default SectionAbout;
