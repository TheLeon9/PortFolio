import React from 'react';
import style from './index.module.scss';

const SectionSkill = () => {
  return (
    <section className={style.section_skill_cont}>

    </section>
  );
};

export default SectionSkill;
