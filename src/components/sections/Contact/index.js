import React from 'react';
import style from './index.module.scss';

const SectionContact = () => {
  return (
    <section className={style.section_contact_cont}>

    </section>
  );
};

export default SectionContact;
